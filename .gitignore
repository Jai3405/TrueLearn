
!.env.example
# Keep sample images outside the repo; these patterns are a backstop, not the control.
# OS
# Python
# Secrets
# Spike outputs — transcripts and per-image results, regenerated on every run
# Student handwriting is children's personal data. Never commit it.
*.heic
*.jpeg
*.jpg
*.png
*.py[cod]
*.pyc
*.webp
.DS_Store
.env
.env.*
.venv/
__pycache__/
app/__pycache__/
spikes/out/
truth.json
venv/
